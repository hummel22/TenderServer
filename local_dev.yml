logging:
  level: INFO
  loggers:
    TenderServer: DEBUG



database:
    # the name of the JDBC driver, mysql in our case
    driverClass: com.mysql.jdbc.Driver

    # the username
    user: tender

    # the password
    password: 12345

    # the JDBC URL; the database is called DWGettingStarted
    url: jdbc:mysql://192.168.56.101:3306/TenderDev

server:
  rootPath: '/api/*'

